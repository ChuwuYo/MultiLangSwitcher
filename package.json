{
	"name": "multilangswitcher",
	"version": "2.1.1",
	"private": true,
	"description": "Chrome extension for switching Accept-Language headers per domain",
	"type": "module",
	"scripts": {
		"lint": "biome lint .",
		"format": "biome format --write .",
		"check": "biome ci .",
		"check:syntax": "node scripts/check-syntax.mjs",
		"test": "vitest run --passWithNoTests",
		"verify": "biome ci . && node scripts/check-syntax.mjs && node scripts/validate-manifest.mjs"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4",
		"vitest": "^2.1.9"
	}
}
